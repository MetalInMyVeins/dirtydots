#!/bin/bash

file="$1"
filenoext=${file%.*}

cython $file
gcc -O2 -fPIC -shared -I$(python3 -c "import sysconfig; print(sysconfig.get_paths()['include'])") $filenoext".c" -o $filenoext".so"
rm $filenoext".c"

